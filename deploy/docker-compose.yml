services:
  ingress:
    image: haproxy:3.2-alpine@sha256:6343ce34a132a5dceaa24767d739df2bd519f8f7c1079ae39e4821334e8eb42e
    restart: unless-stopped
    user: "0:0"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    network_mode: host
    depends_on:
      - cowrie
      - nginx

  collector:
    image: ${PROBING_IMAGE:-ghcr.io/gasserp/probing:main}
    build:
      context: ..
      dockerfile: Dockerfile
      target: probing-collector
    restart: unless-stopped
    command: ["-config", "/etc/probing/config.json"]
    user: "65532:65532"
    read_only: true
    network_mode: none
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - ./config.json:/etc/probing/config.json:ro
      - /var/lib/probing:/data
      - /var/lib/probing/outbox:/outbox
      - nginx-logs:/logs/nginx:ro
      - cowrie-var:/logs/cowrie:ro

  uploader:
    image: ${PROBING_UPLOADER_IMAGE:-ghcr.io/gasserp/probing:uploader-main}
    build:
      context: ..
      dockerfile: Dockerfile
      target: probing-uploader
    restart: unless-stopped
    command:
      - "-account"
      - "${PROBING_STORAGE_ACCOUNT:?set PROBING_STORAGE_ACCOUNT}"
      - "-container"
      - "${PROBING_STORAGE_CONTAINER:?set PROBING_STORAGE_CONTAINER}"
      - "-outbox"
      - "/outbox"
    user: "65532:65532"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - /var/lib/probing/outbox:/outbox
    networks:
      - upload

  nginx:
    image: nginx:1.28-alpine@sha256:a8b39bd9cf0f83869a2162827a0caf6137ddf759d50a171451b335cecc87d236
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    healthcheck:
      test: ["CMD-SHELL", "printf 'PROXY TCP4 127.0.0.1 127.0.0.1 12345 8080\r\nGET /health HTTP/1.0\r\n\r\n' | nc -w 2 127.0.0.1 8080 | grep -qx ok"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
    tmpfs:
      - /var/cache/nginx
      - /var/run
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx-logs:/var/log/nginx/probing
    networks:
      decoy:
        ipv4_address: 172.30.0.3

  cowrie:
    image: cowrie/cowrie:3.0.13@sha256:42e01e0e5fe705a0a63dacc0f1992b2d230740ac149af7675f48197abf740d44
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - ./cowrie.cfg:/cowrie/cowrie-git/etc/cowrie.cfg:ro
      - ./userdb.txt:/cowrie/cowrie-git/etc/userdb.txt:ro
      - cowrie-etc:/cowrie/cowrie-git/etc
      - cowrie-var:/cowrie/cowrie-git/var
    networks:
      decoy:
        ipv4_address: 172.30.0.2

networks:
  decoy:
    internal: true
    ipam:
      config:
        - subnet: 172.30.0.0/24
  upload: {}

volumes:
  nginx-logs:
  cowrie-etc:
  cowrie-var:
